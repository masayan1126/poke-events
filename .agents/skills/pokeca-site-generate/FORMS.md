# Site Generation Form

Ask only for values that cannot be inferred.

## Required

- Event JSON path

## Optional

- Output HTML path, default `index.html`
- Whether this is a live publication run, default `yes`
- Whether to push after generation, default `no`
